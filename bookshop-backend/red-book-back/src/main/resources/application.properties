# -------------------------
# 数据源配置
# -------------------------
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/store?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&autoReconnect=true&allowPublicKeyRetrieval=true
spring.datasource.username=bookuser
spring.datasource.password=1263447196

# -------------------------
# MyBatis 配置
# -------------------------
mybatis.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl

# -------------------------
# Swagger / OpenAPI 配置
# -------------------------
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html

# -------------------------
# CORS 配置（如果你有全局配置类也可以保留）
# -------------------------
# 这里的 CORS 配置如果你在 Java Config 已经写了，可以不用重复
#spring.web.cors.allowed-origins=http://localhost:5173,http://localhost:5174
